const initialState = {
  prevOperand: 111,
  operator: '+',
  currentOperand: 222,
}

const calcReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default calcReducer